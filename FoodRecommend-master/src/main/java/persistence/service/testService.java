package persistence.service;

public class testService {
    public void selectAll(){
//        List<TestDTO> boardDTOS = TestDAO.getRandom();
//        boardDTOS.stream().forEach(v-> System.out.println(v.toString()));
    }
}
